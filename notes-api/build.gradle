plugins {
	id 'java'
    id 'groovy'
    id 'com.github.spotbugs' version '1.6.5'
    id 'com.jfrog.bintray' version '1.7.3'
    id 'maven-publish'
    id "io.spring.dependency-management" version "1.0.5.RELEASE"
}

dependencyManagement {
    imports {
         mavenBom 'org.springframework.boot:spring-boot-dependencies:2.0.1.RELEASE'
    }
}

dependencies {
    compile 'org.slf4j:slf4j-api'
    compile 'com.fasterxml.jackson.core:jackson-annotations'
    compile 'com.fasterxml.jackson.datatype:jackson-datatype-jsr310'
    compile 'io.projectreactor:reactor-core'
    testCompile 'org.codehaus.groovy:groovy-all:2.4.10'
    testCompile 'org.spockframework:spock-core:1.1-groovy-2.4'
    testCompile 'org.hamcrest:hamcrest-core:1.3'
    testCompile 'com.fasterxml.jackson.core:jackson-databind'
    testRuntime 'net.bytebuddy:byte-buddy:1.6.5'
    testRuntime 'org.objenesis:objenesis:2.5.1'
}

task sourcesJar(type: Jar, dependsOn: classes) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

bintray {
    user = 'twinklehawk'
    key = System.getenv('BINTRAY_KEY')
    publish = false
    override = false
    publications = ['mavenJava']
    pkg {
        repo = 'plshark'
        name = 'net.plshark.notes'
        userOrg = 'twinklehawk'
        licenses = ['Apache-2.0']
        websiteUrl = 'https://github.com/twinklehawk/notes'
        vcsUrl = 'https://github.com/twinklehawk/notes.git'
        githubRepo = 'twinklehawk/notes'
        version {
            name = '1.0.0'
        }
    }
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
            artifact sourcesJar
        }
    }
}
